#include <stdio.h>
#include <stdlib.h>

int main()
{
    char nome[256];
    char sobrenome[256];
    int idade;

    printf("Digite seu nome: \n");
    scanf("%s", nome);

    printf("%s",nome);


    return 0;
}
